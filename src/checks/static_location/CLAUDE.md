<static-location-check>
    <desc>
        Checks that every top-level public `static` is defined in an `s` commons module (see import rules check). A public static defined anywhere else is a violation. Private (non-`pub`) statics are allowed anywhere.
    </desc>
</static-location-check>
