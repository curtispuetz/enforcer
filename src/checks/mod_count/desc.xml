<mod-count-check>
  <desc>
    Limits how many modules live at each level. `mod.rs` and `lib.rs` files that declare more than the maximum number of modules (i.e. `mod` declarations) are violations.
  </desc>
  <config toml_table="mod-count">
    <item>max - maximum `mod` declarations allowed per file (default 10).</item>
    <item>
      ignore - list of module paths to skip (the directory containing the `mod.rs`/`lib.rs` the check for, relative to the project root, e.g. `src/app`).
    </item>
  </config>
</mod-count-check>