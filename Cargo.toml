[package]
name = "enforcer"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "cargo-enforcer"
path = "src/main.rs"

[dependencies]
syn = { version = "2", features = ["full"] }
glob = "0.3"
strum = { version = "0.28.0", features = ["derive"] }
