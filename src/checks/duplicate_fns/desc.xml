<duplicate-fns-check>
  <desc>Verifies that there are no duplicate (i.e. alpha-equivalent) free-functions anywhere in the codebase</desc>
  <rationale>To encourage code reuse.</rationale>
  <config toml_table="duplicate-fns">
    <item>
      ignore - list of functions to skip. A file path followed by `::` and the function or `Type::method` (e.g. `src/main.rs::run`, `src/main.rs::Config::new`). Naming any single member of a duplicate group exempts the whole group.
    </item>
  </config>
</duplicate-fns-check>