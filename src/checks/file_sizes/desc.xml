<file-sizes-check>
  <desc>
    Verifies that no source file is too long. A file is a violation if it has more than a maximum number of lines.
  </desc>
  <config toml_table="file-sizes">
    <item>max - max number of lines (default 99)</item>
    <item>ignore - list of exempt files from the rule</item>
  </config>
</file-sizes-check>