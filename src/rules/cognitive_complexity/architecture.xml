<code-architecture>
    The scorer lives in `measure/score.rs` and walks each function body with a `syn::visit::Visit` implementation, carrying a `nesting` depth. To score another branching macro, add its name to `BRANCHING_MACROS` in `measure/score/cnst.rs`; nothing else needs to change.
</code-architecture>