# TODOs

## Checks

- Disallow std certain macros. (Just a preference, I think some of these match code less readable) The list right now:
  - matches
  - assert_matches

## Other

- Consider if it's possible to incorporate shortened imports, for example, if I don't want such a long use statement, I can reexport modules in lib.rs
- Consider if it's possible to require defined types if two or more types are commonly repeated with one another.
- Consider if its possible to make repeated longly written nested type needed to be defined with type alias.
