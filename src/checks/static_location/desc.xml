<static-location-check>
  <desc>
    Verifies that every top-level public `static` is defined in an `s` commons module (see import rules check). A public static defined anywhere else is a violation.
  </desc>
  <note>Private (non-`pub`) statics are allowed anywhere</note>
</static-location-check>