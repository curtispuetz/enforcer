<c-mod-contents-check>
  <desc>
    Verifies the root `mod.rs` of every `c/` directory (see import-rules check):
    - Declares all of its modules as public (`pub mod ...`); and
    - Contains no `use` statements.
  </desc>
  <rationale>
    So users can import the submodules and call the functions (e.g. submodule::fn_name())
  </rationale>
  <exception>
    <desc>
      If the `c/` directory contains a nested `c/` sub-directory, the root `mod.rs` must instead:
    </desc>
    <items>
      <item>
        Declare the nested module privately as `mod c;`, annotated with the outer attribute `#[allow(clippy::module_inception)]`
      </item>
      <item>
        Have a single `pub use c::*;` glob statement re-exporting the nested `c` module.
      </item>
    </items>
  </exception>
</c-mod-contents-check>