<type-location-check>
  <desc>
    Verifies that every top-level public `struct`, `enum`, and `type` alias is defined in a `t` commons module (see import-rules check). A public type defined anywhere else is a violation.
  </desc>
  <note>Private (non-`pub`) types are allowed anywhere</note>
</type-location-check>